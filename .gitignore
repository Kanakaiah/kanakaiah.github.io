# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Reference apparatus samples for scripts/check_cross_references.js. Published
# apparatuses are generally copyrighted, so the samples stay local rather than being
# redistributed here.
samples

# Working output of scripts/generate_chapter_prompts.js — regenerate it rather than
# committing it; the anchors it derives from are already in source.
prompts

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
